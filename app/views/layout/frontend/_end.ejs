</html>

<% if (appConfig?.env !="production") { %>
<script src="https://cdn.jsdelivr.net/npm/eruda"></script>
<script>
    try {
        var query_str = window.location.search;
        if (query_str.indexOf('debug=1') > -1) {
            eruda.init()
        }
    } catch (err) {
        console.log(err)
    }
    eruda.init()
</script>
<% } %>